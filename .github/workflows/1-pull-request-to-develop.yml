name: "01 - [FEAT] Build & PR"

on:
    push:
        branches:
            - 'feature**'

jobs:
    infra:
        uses: org-wantoli/up-nested-reusable-infra-terraform/.github/workflows/feature.yml@v1
        secrets: inherit